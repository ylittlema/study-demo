package ${packPath}mapper;

import ${packPath}entity.${className}Entity;

/**
 * <br>
 * <b>功能：</b>${codeName} Mapper<br>
 * <b>作者：</b>siber.xu<br>
 * <b>日期：</b>${systemDate}<br>
 * <b>详细说明：</b>无<br>
 */
public interface ${className}Mapper extends BaseMapper<${className}Entity> {
	
	
}
